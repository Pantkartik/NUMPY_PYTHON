'''
some of the numpy built in functions are used for the finding and handling of the missing values 

'''


'''
1. np.isnan()   ---->  not a number ( when calculation wrong or missing data , return booleans)


2.np.

'''


import numpy as np 

array=np.array([1,2,np.nan,5,np.nan])
print(np.isnan(array))


# can we compare nan value ? 
'''
no we cant compare the nan values 
'''